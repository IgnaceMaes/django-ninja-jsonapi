# Examples

This folder contains runnable examples for `django-ninja-jsonapi`.

## `minimal_django_project`

A minimal Django project using `django-ninja-jsonapi` with two resources:

- `user`
- `computer`

See `minimal_django_project/README.md` for setup and run instructions.
